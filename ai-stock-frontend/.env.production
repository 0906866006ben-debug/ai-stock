# Production build config for Vercel.
# Next.js reads this automatically during `next build`, so the frontend
# points at the deployed Render backend without needing any Vercel dashboard
# env-var setup. NEXT_PUBLIC_* is a public value (baked into client JS by design).
NEXT_PUBLIC_API_URL=https://ai-stock-backend-akxd.onrender.com
