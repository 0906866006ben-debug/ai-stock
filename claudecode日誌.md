# Claude Code 日誌：加密掃描對齊均值回歸 + Discord 兩段推播

日期：2026-07-07
分支：`001-mvp-stock-analysis`
主題：把加密掃描與 Discord bot 對齊使用者的均值回歸看盤流程

## 背景

使用者的實際做法是迷因幣均值回歸：1h 超買/超賣為前提，價格拉回均線停利。
這個 session 我依序處理三件事：把「超買超賣」定成硬門檻、讓 Discord 一有設定就推播、
把「拉回目標」從 1h EMA12 改成 15m EMA12。第三件做到一半，Codex 接手用更乾淨的
寫法完成，並修正了我對門檻的一處誤判（見下方「與 Codex 的交接」）。

## 我做的（依時間）

### 1. RSI-14 75/25 設為硬門檻（commit `f78bda8`）

使用者原話：「反正我最最最基本的條件就是一定要有超買超賣剩下免談」，並選定
`RSI-14 · 75/25(更嚴)`。

- `route.ts`：把 RSI 從加分項升為必要條件——做空要 1h RSI≥75、做多要 ≤25，沒到 `return`。
- 方向由 RSI 決定（超買→做空、超賣→做多）。
- 前端速查表：RSI 從「加分項」搬到「鐵門檻」。

### 2. Discord bot 兩段推播（commit `6cf9929`）

使用者：「我覺得讓 dc 推播給我 1hr 超買超賣」——不要只在最後 1m 收破那刻才通知。

- `discord_replay_bot.py`：`signal_loop` 從「只推 triggered」改為兩段——
  - 未觸發 → `👀觀察`（1h 超買/超賣，提醒去盯 1m）
  - triggered → `★/◆訊號`（1m 收破）
  - setup 與 trigger 各自冷卻，觸發時一併壓 setup 冷卻避免重複洗版。
- 降 `SCAN_URL` 的 `minQ`，讓過 RSI 門檻的設定都進得來。
- 複盤系統提示詞補上明確 75/25。

### 3. 拉回目標改 15m EMA12（未提交，由 Codex 接手完成）

使用者：「我均值回歸拉回的是 15min ema12」。我開始把 `route.ts` 的 EMA12/偏離從
1h 改算在 15m（新增一支 15m K 線抓取），改到一半 Codex 接手。

## 與 Codex 的交接（誠實記錄）

我在第 1 步時**多加了一道「偏離 z 也要達標」的硬 gate**（`ext_z >= EXT_MIN` 才進名單）。
這其實**誤讀**了使用者：偏離只該影響「肉多肉少/排序」，不該擋掉超買/超賣的幣。
Codex 正確地把這道 gate 拿掉，讓 `1h RSI 75/25` 成為**唯一**入選門檻，偏離 z 只進品質分。

Codex 的版本也比我的乾淨：
- 15m K 線只在「過了 RSI 門檻之後」才抓（省 API），我原本無條件抓。
- 補了型別（`BinanceKline[]` 等），加了 `scan_mode: "rsi_setup_v2"` 方便驗部署。
- 觸發框架統一講 `1m EMA12` 破+守住（我先前殘留 EMA20 的說法）。

我審查 Codex 的三個 commit（`701eb31` / `f7135a1` / `76ca419`）後**認可保留**，
理由：抓對意圖、碼乾淨、守住專案紀律（沒宣稱 edge、沒自動下單、沒碰台股主線）。

## 目前整套邏輯（對齊後）

1. **1h RSI-14 超買≥75 / 超賣≤25** —— 唯一硬門檻，決定進不進觀察名單與方向。
2. **15m EMA12** —— 拉回目標/停利參考，只顯示距離，不擋名單。
3. **1m EMA12 整根實體破+守住** —— 進場扳機（◆無量、★有量，均值回歸不強求放量）。
4. 資費順風 / OI 堆積 / 偏離 z —— 只進品質分排序，不是門檻。

## 驗證

- `npx tsc --noEmit`（前端型別）通過。
- `py_compile discord_replay_bot.py` 通過。
- 正式站 `curl /api/crypto-scan?...` 回 `scan_mode: rsi_setup_v2`、`status: ok`。
- 當下名單 3 檔全為超買做空觀察（TAC RSI87/距15mEMA12 +21.4%、EVAA RSI88/+4.4%、
  CHIP RSI75/+2.8%），品質分正確地讓「肉多」的排前面，印證 RSI 進名單、偏離只排序。

## 剩餘注意事項

- **Discord bot 要重啟**才會載入新的兩段推播與 75/25 文字（常駐程序不會熱更新）。
- 1h RSI 到 75/25 很嚴，多數時候名單 0–2 檔，這是刻意的「少而準」，不是壞掉。
- 這仍是**觀察輔助，非回測完成的策略**；要提訊號品質應另建可回測規則，
  不要再把目標 EMA 或資費塞回入選 gate（重蹈我第 1 步的誤判）。

## Claude Code 自我備註

這個 session 最該記的一課：**使用者說「目標是 X」時，X 是停利參考，不是入選門檻。**
我把「偏離達標」加成硬 gate，等於替使用者多做了一層他沒要的過濾，會漏看他真正想看的
超買/超賣幣。Codex 把它修回來是對的。少替使用者「聰明地」過濾，多給他資訊自己判斷。
